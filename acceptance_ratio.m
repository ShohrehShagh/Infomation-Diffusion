function []=acceptance_ratio(N,pi,L0,a_mat,b_mat,mu1,mu2,sigma1,sigma2,Number_of_samples,data_num,Number_of_batches,Burn_in,r)
diff_alpha=alpha-prev_alpha=
if (~isnan(t) && ~isnan(prev_t))
    prod(diff_alpha)/prod(diff_alpha);
elseif(isnan(t) && isnan(prev_t))
elseif (~isnan(t) && isnan(prev_t))
else
end